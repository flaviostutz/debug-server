const {debug, info, warning, error} = require('./index.js');

debug('oh, my');

info('just some information from our fellows...');

warning('there is a clif just a few steps fr...');

error('we couldn\'t advise that poor guy along the clif in time...');
